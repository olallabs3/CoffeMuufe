CREATE TABLE METODO_PAGO (
ID_METODO_PAGO NUMBER(15) CONSTRAINT PK_METODO_PAGO PRIMARY KEY,
EFECTIVO VARCHAR2(30),
TARJETA NUMBER(30)
);

CREATE TABLE CLIENTE (
ID_USUARIO VARCHAR2(30) CONSTRAINT PK_CLIENTE PRIMARY KEY,
NOMBRE VARCHAR2(30),
CONTRASEÑA CHAR(30),
APELLIDO VARCHARD2(30),
EMAIL CHAR(30),
NACIONALIDAD VARCHAR(30),
TELEFONO NUMBER(9)
);


CREATE TABLE DIRECCIONES (
ID_DIRECCIONES NUMBER(20) CONSTRAINT PK_DIRECCIONES PRIMARY KEY,
CALLE VARCHARD2(30),
NUMERO NUMBER(30),
PISO CHAR(10),
ESCALERA VARCHARD2(20),
CP NUMBER(10)
);


CREATE TABLE PRODUCTO (
ID_PLATO NUMBER(20) CONSTRAINT PK_PRODUCTO PRIMARY KEY,
NOMBRE VARCHAR2(30),
INGREDIENTES VARCHAR2(30),
PRECIO NUMBER(9)
);

CREATE TABLE CATEGORIA (
ID_CATEGORIA NUMBER(20) CONSTRAINT PK_CATEGORIA PRIMARY KEY,
NOMBRE_CATEGORIA VARCHAR2(20)
);

CREATE TABLE PEDIDO (
ID_PEDIDO PRODUCTO(20) CONSTRAINT PK_PEDIDO PRIMARY KEY,
PRECIO NUMBER(30),
NOTAS CHAR(100),
PRODUCTO VARCHARD2(20),
FECHA DATE,
ID_METODOPAGO NUMBER(15),
ID_DIRECCION NUMBER(20),
);

CREATE TABLE LINEAS_PEDIDO (
ID_LINEAS_PEDIDO NUMBER(30) CONSTRAINT PK_LINEAS_PEDIDO PRIMARY KEY,
ID_PEDIDO NUMBER(20),
ID_PRODUCTO NUMBER(20),
CANTIDAD NUMBER(20)
);




