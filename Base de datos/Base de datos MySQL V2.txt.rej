diff a/Base de datos/Base de datos MySQL V2.txt b/Base de datos/Base de datos MySQL V2.txt	(rejected hunks)
@@ -1,68 +0,0 @@
-CREATE TABLE ADDRESS(
-constraint primary key(ADDRESS_ID),
-ADDRESS_ID int(20), 
-STREET VARCHAR(30)not null,
-FLAT VARCHAR(10)not null,
-FLAT_NUMBER INT(30)not null,
-POSTAL_CODE INT(10)
-);
-
-CREATE TABLE CATEGORY (
-constraint primary key(CATEGORY_ID),
-CATEGORY_ID int(20),
-CATEGORY_NAME VARCHAR(20) not null
-);
-
-create table PAY_METHOD (
-constraint primary key(PAY_METHOD_ID),
-PAY_METHOD_ID int(15),
-CASH double (5,2),
-BANK_CARD INT (16) not null
-);
-
-create table CLIENT_ (
-constraint primary key(USER_ID),
-FOREIGN KEY (ADDRESS_ID) REFERENCES ADDRESS(ADDRESS_ID),
-USER_ID VARCHAR(30),
-FIRST_NAME VARCHAR(30)not null,
-LAST_NAME VARCHAR(30)not null,
-PASSWORD VARCHAR(30)not null,   
-EMAIL VARCHAR(30),
-NACIONALILTY VARCHAR(30),
-ADDRESS_ID int(20), 
-PHONE_NUMBER int(10)   
-);
-
-CREATE TABLE PRODUCT (
-constraint primary key(PRODUCT_ID),
-FOREIGN KEY (CATEGORY_ID) REFERENCES CATEGORY(CATEGORY_ID),
-PRODUCT_ID int(20), 
-CATEGORY_ID int(20),
-NAME VARCHAR(30)not null,
-DESCRIPTION VARCHAR(300)not null,
-INGREDIENTS VARCHAR(30)not null,
-PRICE DOUBLE(5,2)not null
-);
-
-CREATE TABLE ORDERS(
-constraint primary key(ORDER_ID),
-FOREIGN KEY (PAY_METHOD_ID) REFERENCES PAY_METHOD(PAY_METHOD_ID),
-FOREIGN KEY (ADDRESS_ID) REFERENCES ADDRESS(ADDRESS_ID),
-ADDRESS_ID int(20),
-ORDER_ID int(20),
-PAY_METHOD_ID int(15),
-PRODUCT VARCHAR(20) not null,
-PRICE DOUBLE(5,2)not null,  
-NOTES VARCHAR(100),   
-ORDER_DATE DATE not null  
-);
-
-CREATE TABLE ORDER_LINES (
-ORDER_LINES_ID int(30),
-constraint primary key(ORDER_LINES_ID),
-FOREIGN KEY (ORDER_ID) REFERENCES ORDERS(ORDER_ID),
-FOREIGN KEY (PRODUCT_ID) REFERENCES PRODUCT(PRODUCT_ID),
-ORDER_ID INT(20),
-PRODUCT_ID INT(20),
-QUANTITY int(20) NOT NULL
-);
